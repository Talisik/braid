{
  "name": "weebdl",
  "version": "1.0.0",
  "description": "Firefox-based network monitoring and anime video downloader",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "echo 'Building WeebDL...' && tsc && echo 'Build completed successfully!'",
    "download": "npm run build && echo 'Starting WeebDL anime downloader...' && node dist/core/download-test.js",
    "dev": "tsc --watch",
    "clean": "rm -rf dist && echo 'Cleaned build files'",
    "postinstall": "echo 'WeebDL dependencies installed. Run: npm run download'"
  },
  "keywords": [
    "firefox",
    "network-monitoring",
    "anime-downloader",
    "playwright"
  ],
  "author": "Potat0c0rn3r",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "playwright": "^1.40.0"
  }
}
